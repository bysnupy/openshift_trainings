{{ ansible_managed | comment }}

127.0.0.1   localhost localhost.localdomain localhost4 localhost4.localdomain4
::1         localhost localhost.localdomain localhost6 localhost6.localdomain6

{% for host in groups[target_group_name] %}
{{ hostvars[host].ansible_default_ipv4.address }} {{ host }} {{ host.split('.')[0] }}
{% endfor %}

{% for host in groups['extra_groups'] %}
{{ hostvars[host].ipv4_address }} {{ hostvars[host].inventory_hostname }} {{ hostvars[host].inventory_hostname_short }}
{% endfor %}
